<?xml version="1.0" encoding="utf-8"?>
<!-- Today's cell: prominent blue border to match in-app "today" highlight.
     Shift type is still visible via text color set dynamically in ShiftWidgetProvider. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#F0FFFFFF" />
    <stroke android:width="2dp" android:color="#1565C0" />
    <corners android:radius="4dp" />
</shape>
